import type { JSONContent } from '@tiptap/core'
import { markdownToDoc } from './md-parse.ts'

/**
 * 首次启动时生成的示例文档，同时作为使用说明。
 *
 * 以 Markdown 编写后交由解析器转换为编辑器 JSON，与「文档即 Markdown」的
 * 设计保持一致，也便于维护。
 *
 * 此处**不写 H1**：标题由文件名承载（读写 .md 时 splitTitle 会提取首个 H1
 * 作为标题），正文内再出现一个会产生重复标题。
 */
const WELCOME_MD = `Quill 是一款本地优先的写作工具，用于在同一个界面中完成速记与结构梳理。
每篇文档都是一个纯 Markdown 文件，保存在本机磁盘上。

## 开始使用

- 行首输入 \`- \` 生成无序列表，输入 \`1. \` 生成有序列表
- \`Tab\` 缩进一级，\`Shift + Tab\` 退回一级
- 任意位置输入 \`/\` 唤出块菜单，可插入标题、表格、代码块、图片
- 输入 \`[[\` 建立指向另一篇文档的链接
- 点击右上角「导图」，将大纲呈现为树状图

## 写作功能

- **专注模式**：仅高亮当前段落，其余淡出
- **打字机模式**：光标锁定在视口的固定位置
- **阅读模式** \`F9\`：转为只读，适合通读全文
- **禅模式** \`F11\`：窗口全屏并隐藏界面元素
- 选中文本后可交由 AI 改写，也可添加批注
- \`Ctrl + P\` 打开命令面板，\`Ctrl + /\` 查看全部快捷键

## 外观设置

字体、字号、行距、栏宽与中文排版参数位于「设置 → 外观」，也可从右侧栏调整。
将鼠标移动至窗口右边缘即可展开右侧栏。

- **首行缩进**：顶格 / 1 字 / 2 字，仅作用于正文段落
- **段落间距**与**字间距**：以 \`em\` 为单位，随字号联动
- **两端对齐**：中文正文启用后右侧齐平

## 窗口布局

四个边栏的行为规则如下：

- 上（标题栏）、下（状态栏）、左（文档栏）：鼠标移动即展开，开始输入时自动收起
- 右（外观栏）：鼠标靠近窗口右边缘时展开

## 文档存储位置

默认存放于 \`%USERPROFILE%\\Documents\\Quill\`，每篇文档对应一个 \`.md\` 文件。
每次保存都会自动产生一次 git 提交，任何一次修改都可以找回。

- 点击标题栏的文件夹图标可直接打开该目录
- 在「设置 → 远程备份」中填写私有仓库地址，即可将文档推送到远端
- \`Ctrl + Space\` 唤出快捷便签，写入的内容进入「收件箱」
- 任意文档都可以固定在桌面上，作为随时查阅的磁贴

## Markdown 语法

文档本身就是 Markdown。在编辑器中写入的每种格式，都会原样保存回 \`.md\` 文件。

| 格式 | 写法 |
|---|---|
| 标题 | \`#\` 到 \`######\`，共六级 |
| 粗体、斜体 | \`**粗体**\`、\`*斜体*\` |
| 删除线、高亮 | \`~~删除线~~\`、\`==高亮==\` |
| 行内代码 | 使用反引号包裹 |
| 无序列表、有序列表 | 行首写 \`- \` 或 \`1. \` |
| 待办 | \`- [ ]\` 未完成，\`- [x]\` 已完成 |
| 引用 | 行首写 \`> \` |
| 代码块 | 使用三个反引号包裹，首行标注语言名可启用语法高亮 |
| 表格 | \`| 列一 | 列二 |\` 后跟一行 \`| --- | --- |\` |
| 分隔线 | \`---\` |
| 链接、图片 | \`[文字](地址)\`、\`![说明](路径)\` |
| 双链 | \`[[另一篇文档]]\` |

文件开头若已包含 YAML 元数据，Quill 会原样保留，且不计入正文排版。

少数写法会被规范化为等价形式：\`*\` 与 \`+\` 列表统一为 \`-\`，\`1)\` 统一为 \`1.\`，
Setext 标题转换为 \`#\` 形式。语义完全一致，内容不会丢失。

## 关于数据

正文内容仅为 Markdown，可被任意编辑器打开。
星标、标签与批注属于元数据，存放于 \`.quill-meta.json\`，不会混入正文。

## 大体量文档

数百万字的文档无法整体载入编辑器。打开这类文档时，Quill 会先扫描章节索引，
再按章节惰性读取正文，并提供「进入编辑器」与「分栏阅读」两种打开方式。
阅读视图中可将当前章节提取为独立文档进行编辑，完成后合并回原文档。`

export const WELCOME: JSONContent = markdownToDoc(WELCOME_MD)
